{
  "name": "estatica",
  "version": "1.0.0",
  "description": "ESTÁTICA — blog personal",
  "scripts": {
    "build": "node build.js",
    "dev": "node build.js && npx serve ."
  },
  "dependencies": {
    "gray-matter": "^4.0.3",
    "marked": "^12.0.2"
  }
}
